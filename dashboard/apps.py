from django.apps import AppConfig


class DashboardConfig(AppConfig):
    """Practitioner self-service. Empty until Phase 6."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "dashboard"
    verbose_name = "Dashboard"
